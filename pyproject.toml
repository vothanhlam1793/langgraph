[project]
name = "langgraph-starter"
version = "0.1.0"
description = "A small LangGraph agent for learning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai>=0.3.0",
    "langgraph>=0.4.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
